# Kustomize base — applies to all environments.
# Resources are generated by: helm template laravel-stack . -f values.yaml | kustomize build
# This kustomization is a placeholder for future base-level patches.
# The overlays select which issuer templates are enabled via Helm values.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
