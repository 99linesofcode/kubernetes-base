name: automatic updates

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  contents: write
  issues: write

jobs:
  automatic-updates:
    uses: 99linesofcode/.github/.github/workflows/automatic-updates.yaml@main
  agent:
    uses: 99linesofcode/.github/.github/workflows/update-agent.yaml@main
